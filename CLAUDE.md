# asterisk-lab - Claude Code Project Guide

`AGENTS.md` is the single source of truth for this repository: read order,
directory ownership, working rules, architecture principles, done criteria,
commit conventions, and the spec/plan workflow all live there.

Read `AGENTS.md` first and follow it. Do not duplicate its rules here - update
`AGENTS.md` instead.

## Commit identity (Claude Code override)

Run this once per session before any git commit:

```bash
git config user.name "Claude" && git config user.email "claude@asterisk-lab.local"
```

@AGENTS.md
