# livekit-agents 0.11+ ships plugins as separate PyPI packages under the
# livekit-plugins-* namespace. The [openai,silero] extras exist on some
# releases but don't cover every needed subpackage — pin the plugins
# explicitly so imports resolve deterministically.
livekit-agents>=0.11,<1.0
livekit-plugins-openai>=0.10
livekit-plugins-silero>=0.7
python-dotenv>=1.0
