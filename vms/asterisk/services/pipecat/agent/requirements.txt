# Pipecat lane — mirrors services/livekit/agent/requirements.txt where
# reasonable (same OpenAI models means overlapping deps). Different framework,
# same STT/LLM/TTS providers.
pipecat-ai[silero,openai]>=0.0.83
openai>=1.50,<2.0
python-dotenv>=1.0
