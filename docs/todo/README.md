# TODO Details

One detail file per `TODO.md` entry, created from
`docs/templates/todo-topic-template.md`. Operator-owned: agents may propose
files here but the operator approves adding, closing, or editing entries.
