# Kickoff Prompt - spec04 LiveKit vs Pipecat Fair Comparison

Read `AGENTS.md`, `PLANS.md`, and the governing spec:

```text
docs/specs/spec04-livekit-pipecat-fair-comparison.md
```

Implement the spec end to end: the Fairness Gate, Paired Quality, Latency
Decision, Reliability (two-row split), and normalized Cost panels on the
voicebot dashboard, plus the `run_id` grouping key and the new
expected-answer fixture. Keep `PLANS.md` updated after meaningful work,
preserve user changes, keep secrets and runtime data out of git, run the
relevant checks (`ruff check`, dashboard verify target), and commit all
agent-made changes with an English Conventional Commit.
