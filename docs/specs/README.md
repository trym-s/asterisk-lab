# Specs

Stable implementation contracts live here as `specNN-topic.md`, created from
`docs/templates/spec-template.md` (rules: `docs/runbooks/spec-rules.md`).

Numbers are never reused; completed specs are never deleted; superseded specs
point to their replacement.
